laravel: "@testbench"

providers:
  - Flagship\FlagshipServiceProvider

migrations:
  - database/migrations

workbench:
  install: true
  discovers:
    web: false
    api: false
    commands: true
    components: false
    factories: true
    views: true
